$(function(){;
  // Hides the Modal
  $('#sendMailModal').modal('hide');

  // Set subject and message to empty string
  $('#subject-text').val('');
  $('#message-text').val('');


  swal( 'Atenção', '<%= @notify_message%>', '<%= @notify_flag %>');

});
